[db_info]
db_host	= localhost
db_user = user
db_pass = pass

[user1]
con_key = con key
con_secret = con secret
key = key
secret = secret
#lists = owner:slug, owner2:slug2, owner3:slug3
lists = None
db = name of db to store tweets
