[db_info]
db_host	= localhost
db_user = user
db_pass = pass

[user1]
con_key = con key
con_secret = con secret
key = key
secret = secret
include_path = path to twitter ids
db = name of db to store tweets
